<div class="hero-section">
  <p class="d-flex justify-content-end">
    <% if current_user %>
      <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path, method: :get %>
    <% end %>
  </p>
  <div class="hero-title d-flex flex-column">
    <div>
    <%= image_tag('usicz-logo.png', width: '200') %>
    </div>
    <div class="big-logo">i!iusicz</div>
    <div class="scroll-down">scroll down <i class="fas fa-angle-double-down"></i></div>
  </div>
</div>

<div class="hero-title d-flex flex-column mt-5">
    <div>
    <%= image_tag('usicz-logo.png', width: '80') %>
    </div>
    <div class="medium-logo">i!iusicz</div>
</div>

<div class="h1 d-flex justify-content-center"> centralize your music in a unique place </div>


<div class="container">
  <div class="row d-flex justify-content-between">
    <div class="cards col-3">1_<br>create your account</div>
    <div class="cards col-3">2_<br>import your songs</div>
    <div class="cards col-3">3_<br>listen to your music</div>
  </div>
  <div class="d-flex justify-content-center">
    <button class="button-black m-5"><a href="<%= songs_path %>">start</a></button>
  </div>

</div>
