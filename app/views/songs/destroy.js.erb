console.log('<%= @song.title %>', 'deleted!');

// Find the song row on the table.
// Remove it

//  Caio ↑ , Daniel ↓

function prevDef(event) {
  event.preventDefault();
}

function removeSong() {
  document.getElementById('linha').addEventListener("click",
    prevDef, false);
}

// below the html
// <input type="button" onclick="removeSong();"- value="Add a click handler that calls preventDefault"/>
export { prevDef }
export { removeSong }
